warning: in the working copy of 'app/api/routes_wnba.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/app/api/routes_wnba.py b/app/api/routes_wnba.py[m
[1mindex af22741..538f18d 100644[m
[1m--- a/app/api/routes_wnba.py[m
[1m+++ b/app/api/routes_wnba.py[m
[36m@@ -1,7 +1,10 @@[m
 from fastapi import APIRouter, Query[m
 import sys, os[m
[32m+[m
[32m+[m[32m# Resolve project root regardless of working directory[m
 _root = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))[m
[31m-sys.path.insert(0, _root)[m
[32m+[m[32mif _root not in sys.path:[m
[32m+[m[32m    sys.path.insert(0, _root)[m
 [m
 router = APIRouter(prefix="/wnba", tags=["WNBA"])[m
 [m
[36m@@ -87,34 +90,31 @@[m [mdef wnba_preview(home: str, away: str, simulations: int = Query(default=10000)):[m
 [m
 @router.get("/predictions")[m
 def wnba_predictions(simulations: int = Query(default=10000)):[m
[31m-    """Returns model predictions for ALL today's WNBA games — no edge filter."""[m
[32m+[m[32m    """Returns model predictions for ALL today's WNBA games, no edge filter."""[m
     from wnba_data      import get_team_stats, TEAM_IDS[m
     from wnba_predictor import WNBAPredictionEngine[m
     from wnba_props     import get_wnba_events[m
     from odds_parser    import american_to_implied[m
 [m
[31m-    engine    = WNBAPredictionEngine()[m
[31m-    events    = get_wnba_events()[m
[31m-    results   = [][m
[32m+[m[32m    engine  = WNBAPredictionEngine()[m
[32m+[m[32m    events  = get_wnba_events()[m
[32m+[m[32m    results = [][m
 [m
     for event in events:[m
         home = event.get("home_team", "")[m
         away = event.get("away_team", "")[m
         if home not in TEAM_IDS or away not in TEAM_IDS:[m
             continue[m
[31m-[m
         home_stats = get_team_stats(home)[m
         away_stats = get_team_stats(away)[m
         if not home_stats or not away_stats:[m
             continue[m
[31m-[m
         pred      = engine.predict(home_stats=home_stats, away_stats=away_stats, simulations=simulations)[m
         implied   = round(american_to_implied(-110) * 100, 1)[m
         e_home    = round(pred.home_win_prob - implied, 2)[m
         e_away    = round(pred.away_win_prob - implied, 2)[m
         best_edge = max(e_home, e_away)[m
         label     = f"{away} @ {home}"[m
[31m-[m
         results.append({[m
             "game":         label,[m
             "bet":          f"{home} ML" if e_home >= e_away else f"{away} ML",[m
